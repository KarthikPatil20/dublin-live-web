import PlaceholderPage from "@/components/PlaceholderPage";

export default function AlertsPage() {
  return (
    <PlaceholderPage
      icon="🔔"
      title="Service Alerts"
      subtitle="GTFS-RT service alerts (ported from gtfs_rt_alerts_service.dart via an /api/alerts proxy). UI scaffolded next."
    />
  );
}
