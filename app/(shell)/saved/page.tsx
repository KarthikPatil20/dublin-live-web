import PlaceholderPage from "@/components/PlaceholderPage";

export default function SavedPage() {
  return (
    <PlaceholderPage
      icon="🔖"
      title="Saved Stops & Locations"
      subtitle="Favourites persist via localStorage (ported from favourites_provider.dart). UI scaffolded next."
    />
  );
}
