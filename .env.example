# Public (safe in browser). Restrict the Mapbox token to your domain in the Mapbox dashboard.
NEXT_PUBLIC_MAPBOX_ACCESS_TOKEN=

# Server-only (NEVER prefix with NEXT_PUBLIC_ — used only inside app/api routes)
TFI_API_KEY=
HERE_API_KEY=
